{
  "name": "rail-transient-inspector",
  "version": "0.1.0",
  "private": true,
  "description": "Offline oscilloscope CSV voltage-dip analysis with explicit evidence boundaries",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "demo:report": "node tools/analyze.cjs --demo --out samples/demo-report.json --html samples/demo-report.html"
  },
  "engines": {"node": ">=22"}
}
