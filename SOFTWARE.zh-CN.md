# Rail Transient Inspector · 电源压降分析工具

**状态：v0.1.0 软件原型；已完成合成数据测试，尚未实机验证。开发过程中使用了 AI 辅助。**

本项目面向一种具体工作：把示波器导出的 CSV 转成可复查的电源压降事件记录。

## 首版已经实现

导入本地 CSV，明确选择时间、电压和可选触发通道；显式换算 s/ms/µs/ns、V/mV；用分开的进入与恢复门限识别事件；保留短事件的排除记录；识别时间轴缺口；标记录制边界上的不完整事件；输出事件相对前一个触发沿的时间差；导出 HTML 与 JSON 报告。

程序只处理上传到当前浏览器内存的本地文件。应用代码没有远端上传、统计请求、云接口或持久存储。文件来源、仪器准确度、探头设置等仍需操作者确认。导出报告可能包含输入文件名和备注，分享前需要检查。

## 怎样打开

打开 `standalone.html` 即可尝试内置的合成波形演示。这个单文件版本已包含所需代码和样式，不需要 npm 安装或 API Key。受本次验证环境的浏览器导航策略限制，界面测试通过 Playwright `set_content` 加载同一文件完成；用户本机浏览器仍需实际确认。

导入自己的 CSV 时，先选择需要跳过的前导行，再确认各列和单位。示例的 3.3 V、3.0 V 和 3.08 V 仅为分析参数，不对应任何已核实芯片的复位规格。

## 开源来源与本项目的贡献

CSV 解析使用 Papa Parse 5.5.3，保留原作者 Matthew Holt 的 MIT 许可声明，文件保持上游原样。新增内容是单位与输入检查、压降分析算法、采样缺口处理、触发关联、界面、报告、命令行和测试。

因此，本项目适合标注为“基于开源组件的个人工程工具开发”，上游解析器的作者身份不属于本项目。此处没有复制公司案例、客户资料或他人的 PCB 设计。

## 验证记录

准备环境中 38 项 Node 自动化测试通过。浏览器冒烟检查覆盖了演示、参数修改后禁止导出旧结果、JSON 下载、单位未确认时阻止分析、有效/无效 CSV 和 390 像素窄屏。该检查期间没有观察到网络请求。

未完成部分：独立人工工程复核、真实示波器 CSV 核验、仪器误差与探头设置验证。合成波形、演示图和报告中的数值均不能包装成真实产品实测结果。

代码由 AI 辅助生成并执行了自动测试。维护者完成独立复核后，才能据实增加“本人复核”“真实波形验证”等记录。

## 数据解释边界

事件持续时间从低于进入门限到达到恢复门限，包含滞回带内的时间。门限交点采用线性插值，时间分辨率仍受采样条件限制。采样缺口两侧不做插值，不跨缺口匹配触发。靠近记录边界或缺口的事件只能给出观测到的持续时间。触发时间关联不能证明复位发生或因果关系。

遇到格式不一致、缺失值、重复时间戳时，程序停止并报告，不自行删除有问题的采样点。

## 对外介绍的准确表述

> 我维护一个基于开源 CSV 解析组件的电源瞬态分析原型，新增了滞回事件识别、采样缺口处理、触发时间关联和可追溯报告。实现使用了 AI 辅助，目前完成了合成数据与软件测试，真实仪器核验尚未完成。

不要把它描述为已在客户产品量产使用、已认证电源分析仪或已解决具体 MCU 的复位问题。

## 仓库构建

GitHub Actions 的实际结果见仓库 Actions 页面。构建会运行 Node 测试、Chromium 冒烟检查，并生成单文件演示、合成 CSV、示例报告和真实应用截图。初次源码检出尚无 `standalone.html` 时可运行 `python3 tools/build_demo.py`；它会校验锁定版本的开源解析器。截图中的波形始终是合成数据，实物验证仍未开展。
